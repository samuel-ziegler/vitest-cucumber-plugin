{
    "type": "module",
    "devDependencies": {
        "lodash": "^4.17.21",
        "vitest": "^4.1.0"
    },
    "scripts": {
        "test": "npx vitest run",
        "postinstall" : "ln -s ../../.. node_modules/vitest-cucumber-plugin"
    }
}
